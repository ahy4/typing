---
name: validate-sentences
description: src/lib/sentences.tomlの既存お題を全件検証し、不適なものを削除する。類似重複の検出→JP-kana対応のLLM検証→エンジン検証の順で処理し、無効・重複文を削除してコミットする。
---

# お題一括検証スキル

`src/lib/sentences.toml` に登録済みのお題を全件検証し、不適なものと似すぎているものを削除する。

## 実行環境の前提

**このスキルは parent agent（最上位エージェント）からのみ動作する。** subagent から呼ばれた場合は処理を中断し、ユーザーに「このスキルは parent agent から呼んでください」と報告して終了する。

必要なツール:

- `Read` — ファイル読み込み
- `Write` — 一時ファイル書き出し
- `Edit` — sentences.toml からの削除
- `Bash` — スクリプト実行・`npm run gen`
- **`Agent`** — LLM 検証サブエージェントの dispatch

cwd は **リポジトリルート**（`package.json` がある階層）で実行する。

### CLAUDE.md との衝突回避

ファイル内容の確認は **必ず `Read` ツールを使う**（Bash 経由の `grep` / `cat` は使わない）。

## ステップ 1: sentences.toml の読み込みとパース

`Read` ツールで `src/lib/sentences.toml` を読み込み、全 `[[sentences]]` エントリを抽出する。

各エントリは以下の形式:

```toml
[[sentences]]
jp = "..."
kana = "..."
```

抽出した `{jp, kana}` ペアのリストを作成し、**0 始まりの連番 index** を振る。

エントリ数が 0 件の場合はここで終了し「sentences.toml にエントリがありません」と報告する。

## ステップ 2: 類似重複の検出

**目的**: 内容・表現が酷似していてタイピングゲームとして冗長なお題を特定する。

### フェーズ構成

類似検出は **2 フェーズ**で行う：

- **フェーズ A（広め検出）**: haiku × 3 並列。3 つのうち **1 つでも** `discard` に指摘した文を「削除候補」とする
- **フェーズ B（吟味）**: 削除候補を haiku × 2 並列で再審査。**両方が削除すべき**と判定した文のみ最終的に除外する

### フェーズ A: 広め検出（3 並列）

- 1 バッチあたり最大 **50 文**（件数が 50 以下なら 1 バッチ）
- バッチ数 `B = ceil(全文数 / 50)`
- **バッチをまたぐペアは検出されない**（バッチ内の比較のみ）。件数が 50 以下なら 1 バッチに全件入るので問題ない

全バッチ × 3 個（計 `3B` 個）のサブエージェントを **1 つのメッセージで同時起動**する。

```
Agent(
  description: "Detect similar sentences (batch <i>/<B>, detector <1|2|3>)",
  subagent_type: "general-purpose",
  model: "haiku",
  prompt: "<下記の類似検出プロンプト本文（プレースホルダー置換済み）>"
)
```

### 類似検出プロンプト本文

~~~
あなたは日本語タイピングゲーム用のお題データを検査するアシスタントです。
以下の文リストの中から、**内容・意味・表現が酷似していて重複しているペア**を見つけてください。

## 「酷似」の定義

以下のいずれかに該当するペアを報告する：

- **内容が実質同じ**: 言い回しが多少違っても意味・シチュエーションが同一
  - 例: 「電車の中でスマホを見ている」「電車に乗りながらスマホを操作する」
- **kana が非常に似ている（タイピング体験として冗長）**: kana の編集距離が短い（3 文字以内の差）かつ内容も近い
  - 例: `でんしゃのなかでほんをよんでいる` と `でんしゃのなかでほんをよんでいた`（末尾の「いる/いた」だけ違う）
- **ことわざ・慣用句が同一**: 同じことわざや慣用句を含む文が複数ある

## 「酷似」に **該当しない** もの

- 同じ単語・場所・物が登場するが、場面や行為が明確に異なる
  - OK 例: 「朝にコーヒーを飲む」と「コーヒーを入れる手順が好きだ」（行為・視点が異なる）
- 内容は近いが kana が十分に異なる（5 文字以上の差）

## 出力形式

**JSON 配列のみを返してください。説明文・コードフェンスは付けないでください。**

類似ペアが見つかった場合:
[
  {
    "keep": 0,
    "discard": 3,
    "reason": "内容がほぼ同一（電車でスマホを見る行為）"
  },
  ...
]

類似ペアが 1 件もない場合:
[]

- `keep`: 残す文の index（より自然・多様・タイピング的に価値が高い方）
- `discard`: 削除する文の index

## 対象データ

<SENTENCES_JSON>
~~~

**プレースホルダーの置き換え:**
- `<SENTENCES_JSON>` → バッチ i の文を JSON 配列で渡す（`[{"index": 0, "jp": "...", "kana": "..."}, ...]`）
  - **index はファイル全体での通し番号**（バッチをまたいで一意）

### 応答前処理

haiku モデルはコードフェンスを付けることがある。JSON.parse 前に:
1. 先頭の ` ```json ` / ` ``` ` / 末尾の ` ``` ` を除去
2. 残った文字列を JSON.parse する

### フェーズ A の結果統合 → 削除候補の確定

各バッチ i の 3 つの結果を照合する：

- detector 1・2・3 のうち **1 つでも** ある index を `discard` に指定していれば「削除候補」とする
- 同じ index が **いずれかの detector の `keep`** と **いずれかの detector の `discard`** の両方に現れた場合（detector 間で意見が分かれた場合）は `keep` を優先し、候補から外す
- 削除候補が 0 件なら フェーズ B をスキップして類似重複除外セット = 空とする

### フェーズ B: 吟味（削除候補を 2 並列で再審査）

削除候補リストを 1 つのプロンプトにまとめ、**2 つのサブエージェントを同時起動**する。

```
Agent(
  description: "Review similar-sentence candidates (reviewer <1|2>)",
  subagent_type: "general-purpose",
  model: "haiku",
  prompt: "<下記の吟味プロンプト本文>"
)
```

#### 吟味プロンプト本文

~~~
あなたは日本語タイピングゲーム用のお題データを審査するアシスタントです。
以下の文ペアは「類似しすぎている可能性がある」として候補に挙げられました。
それぞれのペアについて、**本当にタイピングゲームから削除すべきか**を判定してください。

## 削除すべき基準

- 内容・意味・シチュエーションが実質同一で、どちらかが完全に冗長
- kana が 3 文字以内の差しかなく、かつ内容も同じ

## 残すべき基準（削除しない）

- 同じ単語が出てくるが行為・視点・場面が異なる
- kana が 4 文字以上異なる
- 一方がタイピング的に明らかに価値がある（長さや文字種の多様性）

## 出力形式

**JSON 配列のみを返してください。説明文・コードフェンスは付けないでください。**

[
  {"discard": 3, "delete": true, "reason": "内容が同一で冗長"},
  {"discard": 7, "delete": false, "reason": "場面が異なるので残す"},
  ...
]

各候補について必ず 1 エントリ出力すること。

## 審査対象

<CANDIDATES_JSON>
~~~

**プレースホルダーの置き換え:**
- `<CANDIDATES_JSON>` → 削除候補を以下の形式で渡す（`reasons` は discard を指摘した detector の理由をすべて収集した配列）:
  `[{"keep": {"index": K, "jp": "...", "kana": "..."}, "discard": {"index": D, "jp": "...", "kana": "..."}, "reasons": ["detector 1 の理由", "detector 2 の理由", ...]}, ...]`
  - reasons 配列には `discard` を指定した detector の理由のみ含める（`keep` 側や「類似なし」と判定した detector の理由は含めない）

#### フェーズ B の統合ルール

- **reviewer 1・2 の両方が `delete: true`** → 最終的に削除対象とする
- どちらか 1 つでも `delete: false` → 削除しない（残す）

全バッチのフェーズ B 結果を統合し、類似重複として除外する index セットを確定する。

## ステップ 3: JP-kana 対応検証

**目的**: jp の途中で kana が切れていないか、誤読・促音便ミス・複合語の読み誤りがないかを LLM で確認する。

ステップ 2 で除外された文はスキップし、残りの文のみを対象にする。

類似検出と同じ **2 フェーズ構成**で行う：

- **フェーズ A（広め検出）**: haiku × 3 並列。3 つのうち **1 つでも** `valid: false` を返した文を「削除候補」とする
- **フェーズ B（吟味）**: 削除候補を haiku × 2 並列で再審査。**両方が削除すべき**と判定した文のみ最終的に LLM-reject とする

### フェーズ A: 広め検出（3 並列）

- 1 バッチあたり最大 **15 文**
- バッチ数 `M = ceil(検証対象文数 / 15)`

全バッチ × 3 個（計 `3M` 個）のサブエージェントを **1 つのメッセージで同時起動**する。

```
Agent(
  description: "Validate jp-kana (batch <i>/<M>, validator <1|2|3>)",
  subagent_type: "general-purpose",
  model: "haiku",
  prompt: "<下記の検証プロンプト本文（プレースホルダー置換済み）>"
)
```

### 検証プロンプト本文

~~~
あなたは日本語タイピングゲーム用のお題データを検証するアシスタントです。
以下の文リストについて、各文の `kana` が `jp` の**正確かつ完全な**読みになっているかを判定してください。

## 判定基準

各文について次をすべて確認する：

1. **完全性**: kana は jp の最初から最後まで全文字を読んでいるか（途中で切れていないか）
2. **漢字の読み（最重要）**: jp の漢字・複合語が文脈上正しいひらがな読みで kana に含まれているか
   - **複合語・熟語は複合語としての読みを使う**。構成漢字を個別に訓読みしたものを繋げただけの読みは誤り
     例: 夜空 → よぞら（× よるそら）、木漏れ日 → こもれび（× きもれひ）、雪解け → ゆきどけ（× ゆきとけ）
   - **特に「訓読み+訓読み」の複合語に注意**。各漢字の単独の読みを並べただけだと間違いになることが多い
     例: 夕立 → ゆうだち（× ゆうたつ）、土産 → みやげ（× つちさん）、景色 → けしき（× けいしょく）
   - **訓読みが複数ある漢字は単語ごとの正しい読みを使う**。別の活用語の読みを誤適用しない
     例: 冷たい → つめたい（× ひえたい。「ひえ」は「冷える」の読みであって「冷たい」の読みではない）
   - **促音便**（っ）に注意：熟語・複合語では「つ」が「っ」に変わる
     例: 熱唱 → ねっしょう（× ねつしょう）、発表 → はっぴょう（× はつひょう）、切符 → きっぷ（× きつぷ）
   - **文脈に沿った読みになっているか**: 同じ漢字でも文中での意味・品詞によって読みが変わる場合、文脈に合った読みか確認する
3. **ひらがなの一致**: jp のひらがなはそのまま kana に含まれているか
4. **英数字の一致**: jp の英字（大文字も）が小文字で kana に含まれているか、数字がそのまま含まれているか
5. **カタカナの変換**: jp のカタカナがひらがなに変換されて kana に含まれているか（ー は ー のまま OK）
6. **標準的な読み方が2つある漢字・単語の禁止**: jp にそのような漢字・単語が含まれていれば `valid: false` にする
   - タイピングゲームでは読みが一意でないと正解の打鍵列が確定できないため、これらは使用禁止
   - 禁止の代表例: 良い（よい/いい）、日本（にほん/にっぽん）、明日（あした/あす）、一日（いちにち/ついたち）、上手（じょうず/うわて）、下手（へた/したて）、人気（にんき/ひとけ）、白髪（しらが/はくはつ）、銀杏（ぎんなん/いちょう）、重複（じゅうふく/ちょうふく）
   - 上記リストに限らず、標準的な読みが2つある漢字・単語を含む文は除外する
7. **フォーマット違反**: 以下のいずれかに該当する場合も `valid: false` にする
   - kana に漢字が含まれている
   - kana にカタカナが含まれている（ー は OK）
   - kana に大文字 ASCII が含まれている
   - jp または kana に鍵カッコ「」『』が含まれている
   - jp または kana に句読点「。」「、」が含まれている
   - kana にスペースが含まれている
   - kana の長さが 30 文字以上（kana.length ≥ 30）
   - kana が全て英語（日本語を含まない）

## 注意：助詞・一般的な読みを誤って invalid にしない

以下はよくある**誤判定パターン**。これらは valid: true が正しい：

- 助詞「は」「が」「を」「で」はひらがなのまま kana に書く。発音通りの「わ」「お」等に変換しない
  - 正: `象は鼻が長い` → `ぞうははながながい`（「は」は「わ」にしない）
- 一般的な漢字の標準読みは valid: true にする。**自信がなければ valid: true にする**
  - 充電 → じゅうでん ✓（「ちゅうでん」ではない）
  - 聴く → きく ✓（「ちょうく」などとしない）
  - 着く → つく ✓
- 同じ index を 2 回出力してしまった場合は、**最初の判定を採用して 2 回目は無視する**

## NG の例

| jp | kana | 理由 |
|---|---|---|
| 毎朝のコーヒーは欠かせない習慣です | まいあさのこーひーはかかせない | 「習慣です」→「しゅうかんです」が欠落 |
| USB接続がうまくいかない | usbせつぞくがうまく | 「いかない」が欠落 |
| 桜が満開になる季節は美しい | さくらがまんかいになるきせつは | 「は美しい」→「はうつくしい」が欠落 |
| 友達とカラオケで熱唱した | ともだちとからおけでねつしょうした | 「熱唱」は促音便で「ねっしょう」が正しく「ねつしょう」は誤読 |
| 発表の準備をする | はつひょうのじゅんびをする | 「発表」は「はっぴょう」が正しく「はつひょう」は誤読 |
| 夜空に星がいっぱい輝いている | よるそらにほしがいっぱいかがやいている | 「夜空」は複合語で「よぞら」が正しく「よるそら」（夜=よる・空=そら の個別読みの連結）は誤読 |
| 冷たいアイスが食べたい気分だ | ひえたいあいすがたべたいきぶんだ | 「冷たい」は「つめたい」が正しく「ひえたい」は誤読（「冷える」の読みを「冷たい」に誤適用） |
| 図書館で本を3時間勉強した | としょかんでほんを3じかんべんきょうした | jp自体が不自然（「本を勉強する」は誤用。「本」は勉強の手段なので「を」ではなく「で」または文自体を「図書館で3時間勉強した」等に直す） |

## 出力形式

**JSON 配列のみを返してください。説明文・コードフェンスは付けないでください。**

[
  {"index": 0, "valid": true},
  {"index": 1, "valid": false, "reason": "「習慣です」に対応する「しゅうかんです」が kana に含まれていない"},
  ...
]

## 対象データ

<SENTENCES_JSON>
~~~

**プレースホルダーの置き換え:**
- `<SENTENCES_JSON>` → バッチ i の文を JSON 配列で渡す（例: `[{"index": 0, "jp": "今日はいい天気だ", "kana": "きょうはいいてんきだ"}, ...]`）
  - **index はファイル全体での通し番号**（バッチをまたいで一意）

### 検証 subagent の応答前処理

haiku モデルはコードフェンスを付けることがある。JSON.parse 前に必ず以下の前処理を行う:

1. 先頭の ` ```json ` / ` ``` ` / 末尾の ` ``` ` を剥ぐ（正規表現で `^\s*```(?:json)?\s*` と `\s*```\s*$` を除去）
2. 残った文字列を JSON.parse する

### フェーズ A の結果統合 → 削除候補の確定

各バッチ i の 3 つの結果を照合する：

- validator 1・2・3 のうち **1 つでも** ある index を `valid: false` としていれば「削除候補」とする
- **同一 validator が同一 index を複数回出力した場合** → 最初の判定を採用
- 削除候補が 0 件なら フェーズ B をスキップし LLM-reject = 空とする

### フェーズ B: 吟味（削除候補を 2 並列で再審査）

削除候補リストをまとめ、**2 つのサブエージェントを同時起動**する。

```
Agent(
  description: "Review jp-kana candidates (reviewer <1|2>)",
  subagent_type: "general-purpose",
  model: "haiku",
  prompt: "<下記の吟味プロンプト本文（プレースホルダー置換済み）>"
)
```

#### 吟味プロンプト本文

~~~
あなたは日本語タイピングゲーム用のお題データを審査するアシスタントです。
以下の文は「kana が誤っている可能性がある」として候補に挙げられました。
それぞれについて、**本当に削除すべきか**を最終判定してください。

## 削除すべき基準

- kana が jp の途中で切れている（末尾の語が kana に含まれていない）
- 漢字・複合語の読みが明らかに誤っている（促音便ミス・複合語の個別読み連結など）
  例: 熱唱 → ねつしょう（× ねっしょう）、夜空 → よるそら（× よぞら）
- フォーマット違反: kana に漢字・カタカナ（ー 以外）・大文字 ASCII・句読点・スペースが含まれる、または kana.length ≥ 30、または kana が完全英語
- 標準的な読みが 2 通りある漢字・単語を jp が含んでいる

## 残すべき基準（削除しない）

- kana が jp を正確に最初から最後まで読んでいる
- 助詞「は」「が」「を」「で」はひらがなのまま kana に書く（「わ」「お」に変換しない）
- 一般的な漢字の標準読みは削除しない。**自信がなければ delete: false にする**

## 出力形式

**JSON 配列のみを返してください。説明文・コードフェンスは付けないでください。**

[
  {"index": 1, "delete": true, "reason": "「習慣です」に対応する「しゅうかんです」が kana に含まれていない"},
  {"index": 5, "delete": false, "reason": "kana は jp を正確に読んでいる"},
  ...
]

各候補について必ず 1 エントリ出力すること（漏れ禁止）。

## 審査対象

<CANDIDATES_JSON>
~~~

**プレースホルダーの置き換え:**
- `<CANDIDATES_JSON>` → 削除候補を以下の形式で渡す:
  `[{"index": N, "jp": "...", "kana": "...", "reasons": ["validator 1 の理由", "validator 2 の理由", ...]}, ...]`
  - `reasons` 配列には `valid: false` を返した validator の理由のみ含める（`valid: true` の validator の理由は含めない）

#### フェーズ B の統合ルール

- **reviewer 1・2 の両方が `delete: true`** → LLM-reject として記録
- どちらか 1 つでも `delete: false` → 残す

全バッチのフェーズ B 結果を統合し、LLM-reject の index セットを確定する。

## ステップ 4: フォーマット・エンジン検証（validate-sentences.ts）

ステップ 2（類似重複）とステップ 3（LLM-reject）で除外されなかった**有効**な文をエンジンでも検証する。

### 4-a. JSON ファイルを保存

有効文リストを `JSON.stringify(sentences)` で文字列化し、**`Write` ツールで** `gomi/sentences_to_validate.json` に書き出す。

- `gomi/` が存在しない場合は `Bash` で `mkdir -p <repo-root>/gomi` を実行して作成する
- Bash 経由の `node -e` は使わない（必ず `Write` ツール）

形式例:
```json
[{"jp": "今日はいい天気だ", "kana": "きょうはいいてんきだ"}, ...]
```

### 4-b. 検証スクリプトを実行

```bash
node --experimental-strip-types scripts/validate-sentences.ts gomi/sentences_to_validate.json
```

終了コードに応じた処理：
- `0` → 全文有効
- `1` → stdout に出力される `=== Errors (discard) ===` セクションから NG 文の `jp` を抽出し、エンジン-reject として記録する\
  行フォーマット: `  [jp文字列] 理由テキスト`（`[` と `]` の間が jp、`]` の後にスペース + 理由テキストが続く）\
  抽出正規表現: `/^\s+\[(.+?)\]/` の第1キャプチャグループが jp。複数行ある場合は全行に適用する

ステップ 2・3・4 の除外をすべて合わせて**除外文セット**を確定する。

## ステップ 5: 除外文を sentences.toml から削除する

除外文セットが 0 件の場合はこのステップをスキップして「除外すべき文はありませんでした」と報告し、ステップ 6 へ。

`Read` ツールで `src/lib/sentences.toml` を再読み込みし、除外文セットに含まれる各エントリを削除する。

### 削除の手順（1 エントリずつ `Edit` で処理する）

各削除対象エントリについて：

```
old_string = "\n[[sentences]]\njp = \"<jp>\"\nkana = \"<kana>\""
new_string = ""
```

ただし、ファイル先頭のエントリ（先頭の改行がない場合）は:

```
old_string = "[[sentences]]\njp = \"<jp>\"\nkana = \"<kana>\"\n"
new_string = ""
```

**ユニーク性確保**: jp または kana が重複しているエントリが存在し Edit がエラーを返した場合は、前後の空行も含めた文字列で再試行する。

削除後、`Bash` で `npm run gen` を実行して `src/lib/generated/sentences.json` を再生成する。

## ステップ 6: コミットと完了報告

除外文が 1 件以上あった場合はコミットする:

```bash
git add src/lib/sentences.toml src/lib/generated/sentences.json
git commit -m "fix(sentences): remove <N> invalid/duplicate entries"
```

コミットメッセージの body に除外した jp のリストと理由（類似重複 / LLM-reject / エンジン-reject の区別付き）を箇条書きで含める。

除外文が 0 件の場合はコミットしない。

### 完了報告

- 検証した総件数
- 類似重複として除外した件数（どのペアを残したか・消したかの概要）
- LLM 検証で除外した件数（理由の内訳サマリー）
- エンジン検証で除外した件数
- 合計除外件数 / 残存件数
- 除外した jp のリスト（各理由付き）
