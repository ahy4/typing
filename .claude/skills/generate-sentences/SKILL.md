---
name: generate-sentences
description: タイピングゲーム用のお題（Sentence[]）を生成し、検証してsrc/lib/sentences.tomlに追記する。件数・1バッチあたりの上限N・JPの長さをユーザーにヒアリングし、生成サブエージェント→JP-kana対応の検証サブエージェント（各バッチ2並列）→エンジン検証の順で処理する。
---

# お題生成スキル

タイピングゲーム用のお題（`Sentence[]`）を並列生成・多段検証し、`src/lib/sentences.toml` に追記する。

## 実行環境の前提

**このスキルは parent agent（最上位エージェント）からのみ動作する。** subagent（Task / Agent 経由で起動された子エージェント）から呼ばれた場合、更に subagent を nest 起動できない環境がほとんどなので、**最初に必ず処理を中断し、ユーザーに「このスキルは parent agent から呼んでください」と報告して終了する**。

必要なツール:

- `Read` — ファイル読み込み（既存 TOML 件数カウント等）
- `Write` — 新規ファイル作成（`gomi/sentences_to_validate.json` 等）
- `Edit` — 既存ファイルへの追記（末尾エントリを置換する形）
- `Bash` — `npm run gen` 等の実行
- **`Agent`（Claude Code の Task ツールに相当）** — `description` / `subagent_type` / `model` / `prompt` の 4 引数を取り、子エージェントを dispatch する。利用可能なツール一覧で見つからない場合は ToolSearch で `select:Agent` または `select:Task` を試し、それでも無ければ「Agent dispatch ツールが必要です」と報告して中断

cwd は **リポジトリルート**（`package.json` がある階層）で実行する。`scripts/validate-sentences.ts` は相対パスで参照される。

### CLAUDE.md との衝突回避

このリポジトリの CLAUDE.md は `ls` / `grep` / `find` 等の Bash コマンドを禁止している場合がある。本スキルでファイル内容を確認するときは **必ず `Read` ツールを使う**（`Bash` 経由の `grep` / `cat` は使わない）。既存 TOML の `[[sentences]]` 件数カウントも Read で全文取得して数える。

## ステップ 1: ユーザーへのヒアリング

以下を確認する（まとめて一度に聞く）。**ユーザーが依頼メッセージですでに3項目すべてを提示している場合はヒアリングをスキップしてよい。**

1. **生成数**: 合計何文生成するか（例: 10、20、50）
2. **N**: 1サブエージェントあたりの最大生成数（例: 10）。サブエージェント数は `K = ceil(生成数 / N)` になる
3. **JP の長さ**: 短め（jp 10文字以内）/ 普通（jp 11〜18文字）/ 長め（jp 19〜25文字）
   - `jp.length`（UTF-16 code unit）で計測。kana の長さは聞かない

出力先は常に `src/lib/sentences.toml` を使用する（聞かない）。

## ステップ 2: お題生成（claude-haiku サブエージェントを並列起動）

`K = ceil(生成数 / N)` 個のサブエージェントを **1つのメッセージで同時起動**する（並列実行）。

- エージェント i（1始まり）が担当する件数: `min(N, 残り件数)`

```
Agent(
  description: "Generate typing sentences (batch <i>/<K>)",
  subagent_type: "general-purpose",
  model: "haiku",
  prompt: "<下記の生成プロンプト本文（プレースホルダー置換済み）>"
)
```

出力にコードフェンスや説明文が混入したら、**最初の `[[sentences]]` から末尾までを切り出す**（それ以外は捨てる）。

### 失敗検出とリトライ

エージェントの出力に `[[sentences]]` が1つも含まれていない場合、そのバッチは失敗とみなす。

**リトライ手順（2段階）:**

1. **全バッチを受け取った後**、失敗バッチをまとめて 1 メッセージで一括再実行する（並列実行を維持するため、バッチごとに個別再実行はしない）
2. 再実行後もまだ失敗しているバッチはスキップし、残りのバッチで得られた文のみで続行する

### 生成プロンプト本文

~~~
# タスク

**<COUNT>件の TOML エントリを今すぐ出力してください。これだけがあなたの仕事です。**
質問・確認・説明は不要。TOML のみ出力してください。

あなたはタイピングゲーム用のお題を生成するアシスタントです。
以下のルールに厳密に従って、<COUNT>文のお題を TOML 形式で出力してください。
**返答は TOML のみ**。説明文・コードフェンス（` ``` `）は付けないでください。

## 出力形式

[[sentences]]
jp = "表示用テキスト（漢字混じり）"
kana = "打鍵列（ひらがな・英数字）"

[[sentences]]
jp = "..."
kana = "..."

各エントリの間には空行を1行入れること。

## jp と kana の対応関係（最重要）

`jp` は表示テキスト（漢字混じり）、`kana` は jp を**全文発音した**打鍵列です。

**kana は jp の完全な読みでなければならない。絶対に途中で切らない。**

jp のすべての文字に対応する読みが kana に含まれていること:
- 漢字 → 正しいひらがな読み
- ひらがな → そのまま
- カタカナ → ひらがなに変換（例: コーヒー → こーひー）
- 英字 → 小文字のまま（例: USB → usb）
- 数字 → そのまま（例: 3 → 3）

jp に漢字があれば kana は必ず jp より文字数が多くなる。
kana の文字数が jp の文字数以下の場合は kana が途中で切れている証拠。

### 対応例

| jp | kana |
|---|---|
| 今日はいい天気だ | きょうはいいてんきだ |
| コーヒーを飲む | こーひーをのむ |
| USB接続する | usbせつぞくする |
| 3時間勉強した | 3じかんべんきょうした |
| 友達と熱唱した | ともだちとねっしょうした |
| 発表の準備をする | はっぴょうのじゅんびをする |
| 切符を買う | きっぷをかう |

### NG の例（禁止）

```
NG: jp を書いてから kana を短く切る
  jp = "毎朝のコーヒーは欠かせない習慣です"
  kana = "まいあさのこーひーはかかせない"  ← 「しゅうかんです」がない！

OK: kana が jp の完全な読み
  jp = "毎朝のコーヒーは欠かせない習慣です"
  kana = "まいあさのこーひーはかかせないしゅうかんです"
```

## jp フィールドのルール

1. **長さの目安**: <JP_LENGTH_INSTRUCTION>（厳守不要、自然な文を優先）
2. **自然な日本語文**であること（意味が通る文）
3. **鍵カッコ「」『』は禁止**
4. **句読点「。」「、」は禁止**（タイピングゲームのお題として既存 sentences と統一する）
5. **標準的な読み方が2つある漢字・単語は禁止**（タイピングゲームでは読みが一意に決まらないと打鍵列が確定できないため）
   - 禁止例（一部）: 良い（よい/いい）、日本（にほん/にっぽん）、明日（あした/あす）、一日（いちにち/ついたち）、上手（じょうず/うわて）、下手（へた/したて）、人気（にんき/ひとけ）、白髪（しらが/はくはつ）、銀杏（ぎんなん/いちょう）、重複（じゅうふく/ちょうふく）
   - 迷ったら使わない。読みが文脈によらず一意に定まる漢字・単語のみ使用する

## kana フィールドの絶対ルール

1. **漢字・カタカナは禁止**。必ずひらがなに変換する
   - NG: `kana = "東京"` / OK: `kana = "とうきょう"`
   - NG: `kana = "コーヒー"` / OK: `kana = "こーひー"`
2. **英単語は小文字のみ**。大文字禁止
   - NG: `kana = "USB接続"` / OK: `kana = "usbせつぞく"`
3. **長音符 `ー` はひらがなと共存 OK**（`こーひー`、`すぽーつ`）
4. **完全英語文は禁止**。必ず日本語を含む
5. **鍵カッコ「」『』は禁止**
6. **スペース禁止**
7. **全角英数字禁止**（半角のみ）
8. **句読点「。」「、」は禁止**（jp と kana の両方で）
9. **長さは 29 文字以内**（kana.length ≤ 29）。超えると自動的にタイピングゲームに使えない

## ん のルール

`ん` の次が母音・y・n・語末の場合 → kana としては `ほんをよむ` のように書けばエンジンが自動判定する

## 多様性の要件（必須）

生成する文は、**内容・カテゴリ・文型をできるだけバラけさせること**。以下のカテゴリから偏りなく選ぶ：

- 日常生活（朝の準備、歯磨き、洗濯、掃除、ゴミ出しなど）
- 食べ物・飲み物（料理名、味の感想、食事シーン、お菓子、飲み物など）
- 自然・季節（天気、桜・紅葉、海・山、動物、虫、星空など）
- スポーツ・運動（競技名、練習、筋トレ、ウォーキング、体の感覚など）
- 技術・IT（PC操作、スマートフォン、アプリ、SNS、インターネットなど）
- 学習・教育（勉強、読書、試験、授業、資格、図書館など）
- 仕事・ビジネス（会議、メール、締め切り、プレゼン、残業など）
- 感情・心理（喜び、驚き、悩み、緊張、安心、後悔など）
- 旅行・観光（交通、ホテル、名所、土産、パスポートなど）
- 買い物・お金（スーパー、値段、セール、支払い、節約など）
- 健康・医療（病院、薬、睡眠、ダイエット、体調など）
- 家族・人間関係（友人、恋愛、家族の会話、近所付き合いなど）
- 趣味・娯楽（映画、音楽、ゲーム、漫画、カラオケ、写真など）
- 乗り物・交通（電車、バス、車、自転車、渋滞、時刻表など）
- 天気・気候（晴れ、雨、台風、雪、気温、湿度など）
- 料理・レシピ（材料、調理手順、火加減、味付けなど）
- 動物・ペット（犬、猫、魚、鳥、飼育、散歩など）
- ことわざ・慣用句（日本語のことわざや慣用句を文に含める）
- 学校・キャンパス（授業、部活、文化祭、給食、修学旅行など）
- 季節行事（正月、節分、お盆、クリスマス、花火大会など）
- ファッション・外見（服、靴、髪型、化粧、コーディネートなど）
- 住まい・インテリア（家具、引っ越し、掃除道具、間取りなど）
- 環境・エコ（ゴミ分別、節電、リサイクル、自然保護など）
- 科学・宇宙（実験、惑星、重力、細胞、化学反応など）
- 歴史・文化（江戸時代、武士、祭り、伝統工芸、城など）
- 食文化・外食（レストラン、居酒屋、ランチ、食レポ、メニューなど）
- 言葉遊び・ユーモア（ダジャレ的表現、皮肉、おかしな状況など）
- ニュース・社会（選挙、物価、少子化、災害対応など）

<COUNT>文生成する場合、同じカテゴリから連続して2文以上取らないこと。また、文の構造（〜する、〜だ、〜ている、〜たい、〜かもしれない、〜てしまった、〜らしい、疑問文、命令文など）も繰り返さず多様にすること。

## チェックリスト（出力前に各文を確認）

- [ ] kana は jp を最初から最後まで完全に音読したものか（途中で切れていないか）
- [ ] 複合語・熟語は複合語としての正しい読みになっているか（構成漢字を個別に訓読みしたものを並べただけになっていないか）
  - 例: 夜空→よぞら（× よるそら）、木漏れ日→こもれび（× きもれひ）、土産→みやげ（× つちさん）
- [ ] 訓読みが複数ある漢字は文中の単語に対応する正しい訓読みを使っているか（別の活用語の読みを誤適用していないか）
  - 例: 冷たい→つめたい（× ひえたい。「ひえ」は「冷える」の読みであって「冷たい」の読みではない）
- [ ] 熟語・複合語の促音便が正しいか（熱唱→ねっしょう・発表→はっぴょう・切符→きっぷ など、「つ」ではなく「っ」）
- [ ] jp の末尾（です・する・ている・だ 等）が kana の末尾に含まれているか
- [ ] jp の英数字（USB, 3 など）が kana にも同じ文字で含まれているか
- [ ] jp に漢字がある場合、kana の文字数は jp の文字数より多いか
- [ ] kana に漢字が含まれていないか
- [ ] kana にカタカナが含まれていないか（ー・は OK）
- [ ] kana に大文字 ASCII が含まれていないか
- [ ] 鍵カッコが含まれていないか
- [ ] 完全英語文でないか
- [ ] 句読点「。」「、」が jp / kana のどちらにも含まれていないか
- [ ] kana の長さが 29 文字以内か（`kana.length ≤ 29`）
- [ ] 助詞の使い方が自然か（動詞と目的語の組み合わせが成立するか）
  - 例: 「本を勉強した」× → 「本で勉強した」「本を読んだ」など自然な形にする（「勉強する」の目的語は学習内容であり「本」は手段なので「を」は不自然）

**返答は TOML のみ**。説明文・コードフェンスは付けないでください。
~~~

**プレースホルダーの置き換え:**
- `<COUNT>` → そのサブエージェントが担当する件数（`min(N, 残り件数)`）
- `<JP_LENGTH_INSTRUCTION>` → 以下のいずれか（`jp.length` の目安、厳守不要）:
  - 短め: `jp.length の目安は 10 以内です`
  - 普通: `jp.length の目安は 11〜18 です`
  - 長め: `jp.length の目安は 19〜25 です`

全 K 個の生成サブエージェントの結果を受け取ったら（失敗バッチは再実行後）、ステップ 3 へ。

## ステップ 3: JP-kana 対応検証（各バッチに 2 つの検証サブエージェントを並列起動）

**目的**: 生成モデルが jp の途中で kana を切っていないか、誤読していないかを LLM で意味的に確認する。

文が得られた各バッチ i について 2 つの検証サブエージェントを起動する。ステップ 2 のリトライで 2 回連続失敗してスキップしたバッチは除外する。有効バッチ数を M とすると **2M 個のサブエージェントを 1 つのメッセージで同時起動**する。

```
Agent(
  description: "Validate jp-kana (batch <i>, validator <1 or 2>)",
  subagent_type: "general-purpose",
  model: "haiku",
  prompt: "<下記の検証プロンプト本文（プレースホルダー置換済み）>"
)
```

### 検証プロンプト本文

~~~
あなたは日本語タイピングゲーム用のお題データを検証するアシスタントです。
以下の文リストについて、各文の `kana` が `jp` の**正確かつ完全な**読みになっているかを判定してください。

## 判定基準

各文について次をすべて確認する：

1. **完全性**: kana は jp の最初から最後まで全文字を読んでいるか（途中で切れていないか）
2. **漢字の読み（最重要）**: jp の漢字・複合語が文脈上正しいひらがな読みで kana に含まれているか
   - **複合語・熟語は複合語としての読みを使う**。構成漢字を個別に訓読みしたものを繋げただけの読みは誤り
     例: 夜空 → よぞら（× よるそら）、木漏れ日 → こもれび（× きもれひ）、雪解け → ゆきどけ（× ゆきとけ）
   - **特に「訓読み+訓読み」の複合語に注意**。各漢字の単独の読みを並べただけだと間違いになることが多い
     例: 夕立 → ゆうだち（× ゆうたつ）、土産 → みやげ（× つちさん）、景色 → けしき（× けいしょく）
   - **訓読みが複数ある漢字は単語ごとの正しい読みを使う**。別の活用語の読みを誤適用しない
     例: 冷たい → つめたい（× ひえたい。「ひえ」は「冷える」の読みであって「冷たい」の読みではない）
   - **促音便**（っ）に注意：熟語・複合語では「つ」が「っ」に変わる
     例: 熱唱 → ねっしょう（× ねつしょう）、発表 → はっぴょう（× はつひょう）、切符 → きっぷ（× きつぷ）
   - **文脈に沿った読みになっているか**: 同じ漢字でも文中での意味・品詞によって読みが変わる場合、文脈に合った読みか確認する
3. **ひらがなの一致**: jp のひらがなはそのまま kana に含まれているか
4. **英数字の一致**: jp の英字（大文字も）が小文字で kana に含まれているか、数字がそのまま含まれているか
5. **カタカナの変換**: jp のカタカナがひらがなに変換されて kana に含まれているか（ー は ー のまま OK）
6. **標準的な読み方が2つある漢字・単語の禁止**: jp にそのような漢字・単語が含まれていれば `valid: false` にする
   - タイピングゲームでは読みが一意でないと正解の打鍵列が確定できないため、これらは使用禁止
   - 禁止の代表例: 良い（よい/いい）、日本（にほん/にっぽん）、明日（あした/あす）、一日（いちにち/ついたち）、上手（じょうず/うわて）、下手（へた/したて）、人気（にんき/ひとけ）、白髪（しらが/はくはつ）、銀杏（ぎんなん/いちょう）、重複（じゅうふく/ちょうふく）
   - 上記リストに限らず、標準的な読みが2つある漢字・単語を含む文は除外する

## 注意：助詞・一般的な読みを誤って invalid にしない

以下はよくある**誤判定パターン**。これらは valid: true が正しい：

- 助詞「は」「が」「を」「で」はひらがなのまま kana に書く。発音通りの「わ」「お」等に変換しない
  - 正: `象は鼻が長い` → `ぞうははながながい`（「は」は「わ」にしない）
- 一般的な漢字の標準読みは valid: true にする。**自信がなければ valid: true にする**
  - 充電 → じゅうでん ✓（「ちゅうでん」ではない）
  - 聴く → きく ✓（「ちょうく」などとしない）
  - 着く → つく ✓
- 同じ index を 2 回出力してしまった場合は、**最初の判定を採用して 2 回目は無視する**

## NG の例

| jp | kana | 理由 |
|---|---|---|
| 毎朝のコーヒーは欠かせない習慣です | まいあさのこーひーはかかせない | 「習慣です」→「しゅうかんです」が欠落 |
| USB接続がうまくいかない | usbせつぞくがうまく | 「いかない」が欠落 |
| 桜が満開になる季節は美しい | さくらがまんかいになるきせつは | 「は美しい」→「はうつくしい」が欠落 |
| 友達とカラオケで熱唱した | ともだちとからおけでねつしょうした | 「熱唱」は促音便で「ねっしょう」が正しく「ねつしょう」は誤読 |
| 発表の準備をする | はつひょうのじゅんびをする | 「発表」は「はっぴょう」が正しく「はつひょう」は誤読 |
| 夜空に星がいっぱい輝いている | よるそらにほしがいっぱいかがやいている | 「夜空」は複合語で「よぞら」が正しく「よるそら」（夜=よる・空=そら の個別読みの連結）は誤読 |
| 冷たいアイスが食べたい気分だ | ひえたいあいすがたべたいきぶんだ | 「冷たい」は「つめたい」が正しく「ひえたい」は誤読（「冷える」の読みを「冷たい」に誤適用） |
| 図書館で本を3時間勉強した | としょかんでほんを3じかんべんきょうした | jp自体が不自然（「本を勉強する」は誤用。「本」は勉強の手段なので「を」ではなく「で」または文自体を「図書館で3時間勉強した」等に直す） |

## 出力形式

**JSON 配列のみを返してください。説明文・コードフェンスは付けないでください。**

[
  {"index": 0, "valid": true},
  {"index": 1, "valid": false, "reason": "「習慣です」に対応する「しゅうかんです」が kana に含まれていない"},
  ...
]

## 対象データ

<SENTENCES_JSON>
~~~

**プレースホルダーの置き換え:**
- `<SENTENCES_JSON>` → バッチ i の文を JSON 配列で渡す（例: `[{"index": 0, "jp": "今日はいい天気だ", "kana": "きょうはいいてんきだ"}, ...]`）

### 検証 subagent の応答前処理

haiku モデルは指示に反してコードフェンスを付けることがある。応答文字列を JSON.parse する前に必ず以下の前処理を行う:

1. 先頭の ` ```json ` / ` ``` ` / 末尾の ` ``` ` を剥ぐ（正規表現で `^\s*```(?:json)?\s*` と `\s*```\s*$` を除去）
2. 残った文字列を JSON.parse する

### 結果の統合ルール

バッチ i の文 j について:
- **両方の検証サブエージェントが `valid: true`** → ステップ 4 へ進む
- **どちらか一方でも `valid: false`** → 除外（LLM-reject として記録）
- **同じ validator が同一 index を複数回出力した場合**（haiku の混乱で起こる）→ **最初の判定を採用**（後の出力は無視）

全バッチの有効文リストを累積したらステップ 4 へ。

## ステップ 4: フォーマット・エンジン検証（validate-sentences.ts）

ステップ 3 を通過した文をまとめて一時ファイルに保存し、エンジン検証を実行する。

### 4-a. JSON ファイルを保存

ステップ 3 を通過した有効文リストを `JSON.stringify(sentences)` で文字列化し、**`Write` ツールで** リポジトリ内の `gomi/sentences_to_validate.json`（絶対パス: `<repo-root>/gomi/sentences_to_validate.json`）に書き出す。

**重要 - 保存先について**:
- `/tmp` は CLAUDE.md ルールで使用禁止の場合がある。必ずリポジトリ内 `gomi/` に置く
- `gomi/` が存在しない場合は `Bash` で `mkdir -p <repo-root>/gomi` を実行して作成する
- `Bash` 経由の `node -e` は日本語クォート/バックスラッシュのエスケープでコケるため使わない（必ず `Write` ツール）

例:

```
Write(file_path: "/abs/path/to/repo/gomi/sentences_to_validate.json",
      content: '[{"jp":"今日はいい天気だ","kana":"きょうはいいてんきだ"}, ...]')
```

### 4-b. 検証スクリプトを実行

```bash
node --experimental-strip-types scripts/validate-sentences.ts gomi/sentences_to_validate.json
```

Bash ツールは exit code ≠ 0 のときエラーとして表示される（`Error: ... (exit code N)` の形）。エラー表示が無ければ `0`、有る場合は表示された数値を見る。**exit 1 でも stdout は出力される**ので、エラー時も stdout を読むこと。

終了コードに応じた処理：
- `0` → 全文有効
- `1` → stdout に出力される `=== Errors (discard) ===` セクションから NG 文の `jp` を抽出し、有効文リストから除外する。残った文を採用する（再実行は不要）\
  行フォーマット: `  [jp文字列] 理由テキスト`（`[` と `]` の間が jp、`]` の後にスペース + 理由テキストが続く）\
  抽出正規表現: `/^\s+\[(.+?)\]/` の第1キャプチャグループが jp。複数行ある場合は全行に適用する

## ステップ 5: 有効な文を sentences.toml に追記し、JSON を再生成する

**有効文が 0 件の場合はここで終了する**: 追記と `npm run gen` の実行をスキップし、「生成数 <合計取得数> 文 / LLM除外 <LLM除外数> 文 / フォーマット除外 <フォーマット除外数> 文 / 有効文 0 件」をユーザーに報告する。

手順：
1. **既存件数のカウントと存在チェック**:
   - 出力先ファイルが **存在する場合**: `Read` ツールで読み、`[[sentences]]` の出現回数を数えて既存件数とする
   - 出力先ファイルが **存在しない場合**: 既存件数 = 0 として扱い、新規作成扱いで進める（エラーにしない）
2. 全バッチの有効文を以下の TOML 形式に変換する（各エントリは先頭に空行を1行つける）:
   ```
   (空行)
   [[sentences]]
   jp = "<jp>"
   kana = "<kana>"
   ```
3. **追記の手段（既存内容は破壊しない）**:
   - **既存ファイルあり**: `Read` で末尾を確認し、ファイル末尾が改行で終わっていなければ追記文字列の先頭にもう一段の `\n` を補う。`Edit` ツールで「既存ファイル末尾の最後の `[[sentences]]` エントリ **3 行**（`[[sentences]]` ヘッダ + jp 行 + kana 行）」を `old_string`、その直後に新エントリを連結した文字列を `new_string` にして置換する。**ユニーク性確保**: 3 行セットなら通常一意になるが、もし同一文が他にもあり Edit がエラーを返したら、直前の空行も含めて 4 行に拡張して再試行。Write で全文上書きはしない（巨大ファイル時のリスク回避）
   - **新規作成**: `Write` ツールでファイル全体（空行を先頭につけずに `[[sentences]]\njp = ...\nkana = ...\n` から始める）を書き出す
   - 追記文が多い場合は複数回に分けて Edit を実行する。1 回の Edit で追記できる上限は **6 文**（`new_string` = old_string 3 行 + 6 文 × 4 行 = 27 行）。2 回目以降の Edit は、前回 Edit の `new_string` の末尾 3 行（最後に追記したエントリ）を `old_string` にする（ファイルを再 `Read` しなくてよい）
4. 追記後、`Bash` で `npm run gen` を実行（cwd はリポジトリルート前提）して `src/lib/generated/sentences.json` を再生成する

ファイルに書き出し後、結果をユーザーに報告する：
- 生成バッチ数（スキップしたバッチがあれば「5バッチ中4バッチ有効」のように明記）/ 合計生成数（スキップバッチを除いて実際に取得できた文数）/ LLM検証除外数 / フォーマット検証除外数 / 合計有効数
- 追記前の既存件数 / 追記後の合計件数
- 出力先パス（TOML）および再生成された JSON パス
